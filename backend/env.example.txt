# Database Configuration
# Option 1: Use individual parameters
DB_HOST=localhost
DB_PORT=5432
DB_NAME=kolkata_scotty
DB_USER=scotty_user
DB_PASSWORD=your_password

# Option 2: Use connection string (alternative to above)
# DATABASE_URL=postgresql://user:password@localhost:5432/kolkata_scotty

# Server Configuration
PORT=3000
NODE_ENV=development
FRONTEND_URL=https://kolkata-scooty-bike-training-r0iv3gqny-r-swetha1473s-projects.vercel.app/
# Session Security
SESSION_SECRET=your-super-secret-session-key-change-this-in-production
JWT_SECRET=your-jwt-secret-key-change-this-in-production

# Google OAuth Configuration
# Get these from: https://console.cloud.google.com/
# 1. Create a project
# 2. Enable Google+ API
# 3. Create OAuth 2.0 Client ID
# 4. Add authorized redirect URI: https://kolkata-scooty-bike-training.onrender.com/api/auth/google/callback
GOOGLE_CLIENT_ID=your-google-client-id
GOOGLE_CLIENT_SECRET=your-google-client-secret
GOOGLE_CALLBACK_URL=https://kolkata-scooty-bike-training.onrender.com/api/auth/google/callback

# Email Configuration (SMTP)
# For Gmail: Use App Password (not regular password)
# For other providers: Use their SMTP settings
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password-or-smtp-password
SMTP_FROM=your-email@gmail.com

# Optional: For production, update these:
# FRONTEND_URL=https://yourdomain.com
# GOOGLE_CALLBACK_URL=https://yourdomain.com/api/auth/google/callback
# NODE_ENV=production
# SESSION_SECRET=use-a-strong-random-secret-here
# JWT_SECRET=use-a-strong-random-secret-here

